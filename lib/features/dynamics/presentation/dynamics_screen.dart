import 'package:flutter/widgets.dart';

class DynamicsScreen extends StatelessWidget {
  const DynamicsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
